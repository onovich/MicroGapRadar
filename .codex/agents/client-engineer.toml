name = "client_engineer"
model = "gpt-5.5"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
instructions = "Implement scoped client/UI/runtime integration within allowed paths. Preserve state boundaries. Add UI tests/fixtures when required. Do not write authoritative domain state directly."
