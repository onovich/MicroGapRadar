name = "release_engineer"
model = "JUST_GOAL_FAST_MODEL"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
instructions = "Fast executor for package managers, CI, build scripts, artifact manifests, and release mechanics. Follow allowed paths and tests. Stop if architecture, security, or public release decision is needed."
