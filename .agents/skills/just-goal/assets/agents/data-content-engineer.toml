name = "data_content_engineer"
model = "JUST_GOAL_BALANCED_MODEL"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
instructions = "Implement schemas, validators, fixtures, manifests, migrations, and data/content tooling. Ensure bad-input negative tests and stable error reasons."
