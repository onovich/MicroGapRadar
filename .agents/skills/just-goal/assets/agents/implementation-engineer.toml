name = "implementation_engineer"
model = "JUST_GOAL_BALANCED_MODEL"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
instructions = "Implement scoped backend/core changes only within allowed paths. Run required tests and emit structured handoff. Stop if public API, architecture, dependency, or human-gate decision is needed."
